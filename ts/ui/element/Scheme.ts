import Colour from "util/Color";

namespace Scheme {
	export const COLOUR_FOREGROUND_PRIMARY = Colour.WHITE;
	export const COLOUR_FOREGROUND_SECONDARY = Colour.fromInt(0xcccccc);
	export const COLOUR_FOREGROUND_TERTIARY = Colour.fromInt(0x999999);
	export const COLOUR_SHADOW = Colour.BLACK;
	export const COLOUR_INPUT = Colour.fromInt(0x30ff7c)
}

export default Scheme;
