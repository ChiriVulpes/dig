export interface IPriorityListMapApi {
    break: boolean;
}
export default class PriorityMap<T> {
    static mapAll<T, RETURN>(lists: PriorityMap<T>[], consumer: (api: IPriorityListMapApi, value: T) => RETURN): RETURN[];
    static mapAll<T, RETURN, API extends IPriorityListMapApi>(lists: PriorityMap<T>[], consumer: (api: API, value: T) => RETURN, api: API): RETURN[];
    private readonly internalMap;
    private readonly priorities;
    hasAny(): boolean;
    get(priority?: number): T | undefined;
    set(value: T, priority?: number): this;
    remove(priority?: number): this;
    clear(): this;
    has(priority?: number): boolean;
    map<RETURN>(consumer: (api: IPriorityListMapApi, value: T) => RETURN): RETURN[];
    map<RETURN, API extends IPriorityListMapApi>(consumer: (api: API, value: T) => RETURN, api: API): RETURN[];
    getPriorities(): readonly number[];
    private addPriority;
    private deletePriority;
}
